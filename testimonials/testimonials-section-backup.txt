===================================================================
TESTIMONIALS SECTION BACKUP (removed 2026-08-26, was empty/placeholder)
===================================================================
To restore: paste the HTML block back into site/index.html between the
"PROCESS DIAGRAM" section and the "CTA" section. Paste the CSS block back
into site/styles.css before the "/* ---------- CTA" line. Add the nav
link back into the <div class="nav-links"> block, and the footer link
back into the <div class="footer-links"> block.
===================================================================


--- NAV LINK (goes back into <div class="nav-links">) ------

      <a href="#testimonials">Testimonials</a>


--- FOOTER LINK (goes back into <div class="footer-links">) -

        <a href="#testimonials">Testimonials</a>


--- HTML (index.html — between the process diagram section and the CTA) ---

  <!-- ============ TESTIMONIALS ============ -->
  <section class="section" id="testimonials">
    <div class="wrap">
      <div class="section-head center reveal">
        <p class="eyebrow">Testimonials</p>
        <h2 class="section-title">Hear from the people we work with.</h2>
        <p class="lead">We've just started — the first results are coming in. Real stories from coaches and course creators will live here soon.</p>
      </div>

      <div class="bento reveal d1">
        <div class="tcard placeholder tall"><div class="quote-mark">&ldquo;</div><div class="ph-label">Your story here</div></div>
        <div class="tcard placeholder"><div class="quote-mark">&ldquo;</div><div class="ph-label">Coming soon</div></div>
        <div class="tcard placeholder"><div class="quote-mark">&ldquo;</div><div class="ph-label">Coming soon</div></div>
      </div>

      <div class="empty-banner reveal d2">
        <span class="badge">In progress</span>
        <p>Working with us right now? <a href="https://calendly.com/peter-sparkaiflow/elevate-your-coaching-and-course" target="_blank" rel="noopener" style="color:var(--accent); border-bottom:1px solid var(--accent-dim);">We'd love to feature your story.</a></p>
      </div>
    </div>
  </section>


--- CSS (styles.css — before the "/* ---------- CTA" line) -----------------

/* ---------- testimonials ---------- */
.bento { display: grid; grid-template-columns: repeat(3, 1fr); grid-auto-rows: 240px; gap: 16px; }
.tcard { position: relative; border-radius: var(--radius); border: 1px solid var(--line); background: var(--panel); overflow: hidden; display: flex; flex-direction: column; justify-content: flex-end; padding: 24px; }
.tcard.tall { grid-row: span 2; }
.tcard.placeholder { align-items: center; justify-content: center; background: radial-gradient(80% 80% at 50% 30%, rgba(154,106,60,0.06), transparent 70%), var(--panel); border-style: dashed; border-color: var(--line-2); }
.tcard.placeholder .quote-mark { font-family: var(--serif); font-style: italic; font-size: 68px; line-height: 0.6; color: var(--mute); opacity: 0.55; }
.tcard.placeholder .ph-label { font-family: var(--mono); font-size: 11.5px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--mute); margin-top: 22px; }
.tcard img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }
.empty-banner { text-align: center; margin-top: 40px; color: var(--soft); font-size: 15px; }
.empty-banner .badge { font-family: var(--mono); font-size: 11.5px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--accent); border: 1px solid var(--accent-dim); background: var(--accent-dim); padding: 6px 14px; border-radius: 999px; display: inline-block; margin-bottom: 16px; }

===================================================================
Note: styles.css previously had this CSS block duplicated (once before
the CTA block, once after it). Only one copy is needed on restore.
===================================================================
